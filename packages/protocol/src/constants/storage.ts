export const MAIN_BRANCH_ID = "main"
export const MAIN_BRANCH_NAME = "Main"

/** The `provider` of a comment the app's own agent wrote. */
export const DAILY_AGENT_PROVIDER = "daily_agent"

/** How much of an actor's `provider` is kept. The value comes from an MCP client we do not control, so it is clamped on read. */
export const ACTOR_PROVIDER_MAX_LENGTH = 64
