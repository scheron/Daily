export const MAIN_BRANCH_ID = "main"
export const MAIN_BRANCH_NAME = "Main"

/** Marker file an external writer (CLI) touches so the running app refreshes; watched by cliSignalWatcher. */
export const CLI_MUTATION_SIGNAL_FILE = ".cli-signal"
