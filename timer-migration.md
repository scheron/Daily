 План замены таймера на ручной ввод времени

     Этап 1: Удаление компонентов таймера 🗑️

     Удалить файлы и директории:
     - src/main/setup/ipc/timer.ts - IPC обработчики таймера
     - src/renderer/src/ui/views/Timer/ - весь каталог с Timer view
     - src/renderer/src/ui/views/Main/.../TimerButton.vue - старая кнопка таймера

     Этап 2: Очистка зависимостей таймера 🧹

     Main Process (4 файла):
     - src/main/windows.ts - удалить createTimerWindow() функцию
     - src/main/app.ts - удалить timer из AppWindows, убрать setupTimerIPC
     - src/main/config.ts - удалить timer window configuration
     - src/main/preload.ts - удалить timer IPC методы из BridgeIPC

     Renderer Process (2 файла):
     - src/renderer/src/router/index.ts - удалить /timer route
     - src/renderer/src/ui/views/Main/Main.vue - удалить deprecated task listeners
     - src/renderer/src/ui/views/Main/.../TaskCard.vue - удалить импорт и использование TimerButton

     Shared Types (1 файл):
     - src/shared/types/ipc.ts - удалить timer IPC interface definitions

     Этап 3: Создание нового компонента ручного учета времени ⏱️

     Создать: TimeTrackingButton.vue

     Новый компонент с функционалом:
     - Показывается только если task.estimatedTime > 0 (как текущая TimerButton)
     - Отображает прогресс-бар (spentTime / estimatedTime)
     - Показывает текущее затраченное время на кнопке
     - При клике открывает popup с:
       - TimePicker для выбора часов и минут (переиспользуем существующий компонент)
       - Кнопка "+" - прибавляет выбранное время к task.spentTime
       - Кнопка "−" - вычитает выбранное время (с защитой от отрицательных значений)
     - Минималистичный дизайн в стиле estimation из PanelEdit.vue

     Этап 4: Интеграция нового компонента 🔌

     Обновить TaskCard.vue:
     - Импортировать TimeTrackingButton вместо TimerButton
     - Заменить в template
     - Доступен для редактирования в любой момент для любой задачи (согласно вашим требованиям)

     Этап 5: Проверка типов и тестирование ✅

     1. Запустить pnpm typecheck:main
     2. Запустить pnpm typecheck:render
     3. Запустить pnpm typecheck:shared
     4. Тестовый запуск pnpm dev
     5. Проверка функционала: прибавление/вычитание времени, прогресс-бар, видимость кнопки

     ---
     Ключевые особенности:
     - ✅ Полное удаление floating timer window
     - ✅ Сохранение estimatedTime функционала (без изменений)
     - ✅ Простой и красивый UI для ручного ввода времени
     - ✅ Переиспользование существующего TimePicker компонента
     - ✅ Сохранение прогресс-бара для визуального отслеживания
     - ✅ Работа с существующим полем spentTime в Task модели
