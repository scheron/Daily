/**
 * Error codes thrown by the local-model catalog/service when callers reference
 * a model that isn't in the catalog or isn't installed.
 */
export enum LocalModelErrorCode {
  UnknownModel = "Unknown model",
}
